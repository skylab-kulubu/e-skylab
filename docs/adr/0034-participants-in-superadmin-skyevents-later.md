# Participant roster is shared superadmin; Skyevents is later

Organizers see attendees on one Participant roster in superadmin, not a per-microsite admin. A public Skyevents site (`events.yildizskylab.com` / skyevents / skyevent) is a later phase; existing microsites stay until then. Forms stay skyforms. Rejected: a new forms product; building Skyevents in this Event-ops loop; splitting the roster across skydays/yildizjam admins as the source of truth.
