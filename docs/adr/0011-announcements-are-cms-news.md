# Announcements are CMS News

Club duyurular are the CMS `News` collection (`cms-backend`, `/cms/collections/News`), not a core-API resource. Superadmin will create and edit News through the CMS API (via Traefik). Core Go does not grow an announcements module. Sites may later render the same collection; inscribed in-page edit remains for site copy and Teams.
