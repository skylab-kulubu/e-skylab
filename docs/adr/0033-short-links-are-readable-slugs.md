# Short links are readable slugs, available wherever a link is created

skyl.app aliases default to a readable slug plus year (`gecekodu`, `skydays2026`), editable by the Event creator. Opaque UUIDs are not the public identity. Any admin surface that creates a link can also create a Short link. skyl.app itself stays a redirect host (ADR 0020), not a logged-in SPA.
