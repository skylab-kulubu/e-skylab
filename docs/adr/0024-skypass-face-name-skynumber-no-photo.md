# SkyPass and Wallet pass show name and skyNumber, not a photo

Yusuf locked the SkyPass card and Wallet pass face to name and skyNumber only. A profile photo (`profilePicture` on User) is not on SkyPass or the Wallet pass. This overrides the student-ID-with-photo industry default: SkyPass is a club membership card, not a campus photo ID. Rejected: putting `profilePicture` on the card or Wallet pass.
