# skyticket-backend is legacy reference only

skyticket-backend is an unused older ticket API with its own JWT and database. It is not part of the new platform. Ticketing lives in the Go core ported from e-skylab, not from skyticket.
